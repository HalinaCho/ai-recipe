# PRD — 우리집 식재료 관리 & 레시피 추천 서비스 (가칭: 냉파고)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v2.3 |
| 작성일 | 2026-08-19 |
| 작성자 | Halina Cho |
| 상태 | M1·M2 구현 완료, M3 착수 전 |
| 프로젝트 성격 | 개인용 실사용 서비스로 시작 → 지인(신혼부부 등) 확장 → 비즈니스 모델 검토 |
| 데이터 소스 | Gmail API, 네이버메일 IMAP, Gemini API, 식품의약품안전처 조리식품 레시피 DB, 쿠팡 파트너스 API, 네이버쇼핑 검색 API, 한국천문연구원 특일 정보 API |

---

## 1. 배경 및 문제 정의

### 1.1 배경

맞벌이·신혼부부 가구에서는 쿠팡·네이버쇼핑 등으로 식재료를 자주 주문하지만, "지금 냉장고에 뭐가 있는지", "뭘 먼저 먹어야 하는지"를 파악하는 건 여전히 사람의 기억에 의존한다. 그 결과 재료가 방치되어 버려지거나, 있는 재료를 두고 또 장을 보는 비효율이 반복된다.

한편 온라인 쇼핑몰의 주문내역 메일에는 "무엇을, 언제 샀는지"에 대한 정보가 이미 축적되어 있다. 이 데이터를 자동으로 수집·구조화하면, 사람이 직접 재고를 입력하지 않아도 "우리집 냉장고 현황"을 실시간에 가깝게 파악할 수 있다.

### 1.2 문제 정의

> **"식재료를 사놓고 뭐가 있는지 몰라서 못 챙겨 먹거나 버리게 된다. 소진 임박 재료부터 활용하는 레시피를 자동으로 추천받고, 부족하면 바로 채워 넣을 수 있는 영양사 같은 서비스가 없다."**

### 1.3 솔루션 컨셉

**Gmail로 수신된 온라인 쇼핑 주문내역 메일을 자동으로 분석해 "우리집 재고"를 구성하고, 구매일 기준으로 먼저 소진해야 할 재료를 우선 반영한 레시피를 추천한다. 이를 바탕으로 주간 식단표를 자동 구성하며, 부족한 재료나 밀키트는 쿠팡·네이버쇼핑으로 바로 연결한다.**

핵심 차별점은 **"재고 입력이 필요 없다"** 는 점이다. 사용자는 그냥 하던 대로 온라인에서 장을 보면, 서비스가 알아서 재고를 인식하고 무엇을 먼저 먹어야 할지 알려준다.

### 1.4 개발 단계 (Phase Roadmap)

사이드 프로젝트 특성상 한 번에 모든 기능을 만들지 않고, 각 단계가 독립적으로 가치를 내도록 4단계로 나눈다.

| Phase | 핵심 가치 | 산출물 |
|---|---|---|
| **Phase 1** | 메일 → 재고 파악 | Gmail 연동, 자동 파싱, 가구 단위 재고 관리 |
| **Phase 2** | 재고 기반 레시피 추천 | 소진임박 우선 매칭, 레시피 큐레이션 |
| **Phase 3** | 주간 식단표 | 자동 배치 + 자유 편집, 영양정보 요약 |
| **Phase 4** | 쇼핑 연결 | 부족 재료·밀키트 → 쿠팡/네이버쇼핑 딥링크 |
| **고도화 (Phase 5+)** | 개인화 | 알러지/선호, 입맛 학습, 밀키트 선호 등 (Open Question 참고) |

### 1.5 비목표 (Non-goals, v1 기준)

- ❌ 레시피 acceptance 기반 개인 입맛 학습 (고도화 이관)
- ❌ 알러지·식단 유형(비건/저염 등) 반영한 추천 (고도화 이관)
- ❌ 재고 정밀 수량 차감(**단위 환산**) — 분수 단위(전량/½/⅓) 소진으로 대체 (§5 FR-05-03·FR-05-04 참고)
- ❌ 실제 장바구니 자동 담기 / 자동 결제 (쿠팡·네이버 모두 관련 공개 API 없음 — §5 FR-18 참고)
- ❌ 네이버 쇼핑 커넥트를 통한 자동화된 수익 채널 (약관상 자동화 미지원 — §5 FR-15-03 참고)
- ❌ 메일 포워딩 규칙·수동 업로드 방식의 메일 연동 (설정 단계가 많거나 사람 개입이 반복돼 타깃 사용자에게 허들이 높음 — §5 FR-01 참고)

---

## 2. 타깃 사용자

### 2.1 페르소나

**P1. 맞벌이/신혼부부 가구 — 시작 사용자 (본인)**

- 온라인으로 식재료를 자주 구매하며, 부부가 각자의 계정으로 주문하는 경우가 있다 (예: 배우자가 쿠팡으로 개별 주문).
- 니즈: 누가 샀든 하나의 "우리집 재고"로 통합해서 보고, 소진 순서에 맞춰 요리하고 싶다.
- 성공 기준: 재고를 수동으로 입력하는 일이 거의 없어야 한다.

### 2.2 사용자 단위 모델

| 개념 | 정의 |
|---|---|
| **가구 (Household)** | 재고·식단표·즐겨찾기를 공유하는 단위. 1가구 = 1개 이상의 구성원(Member) |
| **구성원 (Member)** | 가구에 속한 개인. 로그인 계정(Google 로그인) 기준 1명 |
| **Gmail 연결 (GmailConnection)** | 가구에 연결된 Gmail 계정. **가구당 여러 개 연결 가능** (부부 각자의 Gmail을 모두 연동해, 누가 주문하든 같은 재고로 합산) |

> 동일 상품을 여러 구성원이 각자 주문한 경우, 이는 **정상적인 개별 구매**로 각각 재고에 반영한다 (중복 제거 로직 없음). 다만 동일 메일이 시스템 오류로 두 번 처리되는 것을 막기 위한 **멱등성 처리**(메일 고유 ID 기준)는 별도로 적용한다 (§5 FR-02 참고).

---

## 3. 시스템 아키텍처 개요

```
[Gmail 계정 1..N] ─── OAuth 연결 ──────────┐
                                             │
[네이버메일 계정 1..N] ─ IMAP(앱비밀번호) ─┤   Vercel Cron (매일 자동 폴링)
                                             │   + 수동 동기화 버튼
                                             ▼
                        Gmail: category:purchases(1차) + 발신 도메인(보조)
                        네이버메일: IMAP SEARCH FROM(등록 쇼핑몰 발신 도메인)
                                                       │
                                                       ▼
                                        Gemini API (LLM 기반 파싱)
                                        메일 본문 → 구조화 JSON
                                        (원본 메일 본문은 저장하지 않음)
                                                       │
                                                       ▼
                                    Supabase(Postgres) — 가구 단위 재고 저장
                                     보관방식별 경과율 순 정렬(FR-04-02)
                                                       │
                        ┌──────────────────────────────┼──────────────────────────────┐
                        ▼                               ▼                               ▼
              Phase 2: 레시피 매칭                Phase 3: 주간 식단표            Phase 4: 쇼핑 연결
        식약처 레시피 DB(COOKRCP01)          평일저녁+주말·공휴일 점심/저녁      쿠팡 파트너스(메인)
        + LLM 1회 구조화(주/부재료)          순차 반영 + 카테고리 다양성        네이버쇼핑 검색(보조)
        + 소진임박 가중치 스코어링            + 특일정보 API(공휴일 자동인식)     딥링크만 (자동결제 없음)
```

---

## 4. 정보 구조 및 화면 구성

### 4.1 전체 사이트맵

```
[온보딩 — 최초 1회]
  스플래시 → 구글 로그인 → 가구 생성/합류
  → 메일 연동 (Gmail 또는 네이버메일 선택 → 프로바이더별 단계별 가이드)
  → 첫 동기화 로딩 → "우리집 재고가 다 모였어요!" 완료 화면

[메인 — 하단 탭바 5개]
┌────────┬────────┬────────┬────────┬────────┐
│  홈    │  재고  │ 레시피 │ 식단표 │ 장보기 │
└────────┴────────┴────────┴────────┴────────┘

  🏠 홈
    - 오늘의 추천 레시피 (Phase2 자동 큐레이션)
    - 소진임박 재료 요약 (신선도 카드)
    - 오늘 식단표 미리보기
    - [동기화] 버튼 (수동 동기화, FR-02-02)

  🧊 재고
    - 전체 재고 리스트 (보관방식별 경과율 순, 재료별 아이콘 카드)
    - 항목 탭 → 남은 양 조정·소진 처리 (FR-05-02·FR-05-03), 보관 방식 수정 (FR-04-05)
    - [+ 직접 추가] → 재료 자동완성 + 수량·구매일·보관방식 (FR-04-06·FR-04-07)

  🍳 레시피
    - 온디맨드 전체 레시피 목록 (매칭률순 정렬)
    - 카드 탭 → 상세 (재료/조리법/영양정보/부족재료+쇼핑 CTA)
    - "요리함" 버튼 → 체크리스트 모달 → 재고 반영 (FR-05-01)

  📅 식단표
    - 주간 캘린더 뷰 (평일 저녁 + 주말·공휴일 점심·저녁)
    - 끼니 카드 탭 → 스왑 or 직접 레시피 선택 (FR-12)
    - 하단: 주간 영양정보 요약 (FR-14)

  🛒 장보기
    - 주간 통합 부족재료 리스트, 재료 기준 통합 표시 (FR-17-03)
    - 항목별 [쿠팡에서 보기] / [네이버쇼핑 보기] / [밀키트로 보기]

[설정 — 별도 진입, 탭바에 없음]
  - 연결된 메일 계정 관리 (Gmail/네이버메일 추가·해제)
  - 가구 구성원 관리 (초대 코드)
  - 등록 쇼핑몰 발신 도메인 관리
```

### 4.2 재료 시각 자료(아이콘/일러스트) 확보 전략

실제 상품 이미지는 주문 메일에 포함되어 있지 않아 소싱이 어렵고(Atlas Weather 프로젝트에서 겪은 이미지 소싱 복잡도와 유사), 카테고리 아이콘만으로는 "우유·요거트·치즈"처럼 같은 카테고리 내 항목 구분이 안 된다. 아래 하이브리드 방식을 택한다.

| 구성 | 방식 | 커버리지 |
|---|---|---|
| 기본 재료 (약 80%) | 오픈소스 이모지 세트(Twemoji/OpenMoji SVG) | 우유🥛·계란🥚·양파🧅·마늘🧄·당근🥕·고기🥩·생선🐟 등 서구권에도 통용되는 재료 |
| 한식 특화 재료 (약 20%) | AI 이미지 생성으로 직접 제작 (이모지와 톤이 어울리는 플랫·둥근 스타일로 프롬프트 고정) | 두부·김치·어묵·미역·콩나물 등 이모지가 없거나 어색한 재료 |
| 미매핑 재료 | 카테고리 아이콘 폴백 | 위 두 세트에 없는 신규/희귀 재료 |

정규화된 재료명(`InventoryItem.normalizedName`, FR-03-01에서 LLM이 이미 표준화)을 그대로 매핑 키로 사용하므로, 별도의 매핑 작업 파이프라인이 추가로 필요하지 않다. 데이터 모델은 §6 `IngredientIconMap` 참고.

### 4.3 디자인 톤

"쉬운 앱이지만 촌스럽지 않게" — 토스·당근마켓처럼 큰 타이포와 넉넉한 여백으로 접근성을 확보하되, 2030 사용자에게도 세련되게 읽히는 톤을 지향한다. 상세 팔레트·타이포는 Design.md에서 별도로 다룬다.

---

## 5. 기능 요구사항

### Phase 1 — 메일 → 재고 파악

#### FR-01. 메일 연동 (Gmail + 네이버메일 병렬 지원)

**배경**: 쿠팡은 주문내역 메일 수신 주소를 Gmail로 변경할 수 있지만 설정 단계가 번거롭고, 네이버쇼핑은 네이버 플랫폼 특성상 수신 주소 변경 자체가 불가능하다. 또한 타깃 사용자(5060대 여성 주부 등, §10 Q10 참고)에게는 "메일 주소를 갈아탄다"는 개념 자체가 진입장벽이 될 수 있다. 따라서 사용자가 원래 쓰던 메일함(Gmail 또는 네이버메일)을 그대로 두고, 서비스가 그 메일함에 **읽기 전용으로 연결**하는 방식을 기본으로 한다.

| ID | 요구사항 |
|---|---|
| FR-01-01 | 사용자는 **Gmail(OAuth) 또는 네이버메일(IMAP) 중 하나 이상**을 선택해 연동할 수 있다. 가구당 여러 개의 계정(Gmail·네이버메일 혼합 포함)을 연결할 수 있다 (예: 남편은 쿠팡을 Gmail로 받고, 아내는 네이버메일을 그대로 사용). |
| FR-01-02 | **Gmail**: Google OAuth로 연결한다. 로그인(Supabase Auth)과 Gmail 데이터 접근 권한(`gmail.readonly`)은 별도의 OAuth 플로우로 분리한다. |
| FR-01-03 | **네이버메일**: IMAP으로 연결한다. 사용자는 네이버메일에서 'IMAP/SMTP 사용'을 켜고 발급받은 **앱 비밀번호 1개**를 서비스에 입력하는 것으로 연동을 완료한다 (규칙·필터를 직접 설정할 필요 없음). |
| FR-01-04 | 네이버메일 IMAP 연동 과정은 **단계별 스크린샷 가이드(온보딩 마법사)**로 안내한다. "네이버메일 접속 → 환경설정 → POP3/IMAP 설정 → IMAP 사용 켜기 → 앱 비밀번호 발급" 각 단계를 화면 캡처와 함께 순서대로 보여준다 (타깃 사용자의 기술 허들을 낮추기 위한 핵심 장치). |
| FR-01-05 | **Gmail** 메일 조회는 검색 쿼리 `category:purchases`(2025년 9월~ Gmail 시스템 카테고리)를 1차 필터로, 등록 쇼핑몰 발신 도메인을 보조 필터로 사용한다. |
| FR-01-06 | **네이버메일**은 `category:purchases`에 준하는 자동 분류 기능이 없으므로, IMAP `SEARCH FROM` 명령으로 **등록된 쇼핑몰 발신 도메인(쿠팡, 네이버페이 등)을 1차이자 유일한 필터**로 사용한다. 전체 편지함을 순회하지 않는다. |
| FR-01-07 | 두 방식 모두 접근 범위를 필요한 최소한으로 유지한다 (Gmail은 Google의 민감 범위 심사 부담 최소화, 네이버메일은 발신자 필터링으로 편지함 전체 노출 최소화). |

#### FR-02. 메일 동기화

| ID | 요구사항 |
|---|---|
| FR-02-01 | 매일 자동으로 신규 메일을 확인한다 (Vercel Cron, 새벽 시간대 1일 1회). |
| FR-02-02 | 사용자가 원할 때 수동으로 즉시 동기화할 수 있는 버튼을 제공한다. |
| FR-02-03 | 동일 메일 고유 ID(Gmail 메시지 ID 또는 IMAP UID)가 이미 처리된 경우 재처리하지 않는다 (멱등성 보장 — 크론 중복 실행이나 재전송 메일로 인한 중복 반영 방지 목적). |

#### FR-03. 메일 파싱 (LLM 기반)

| ID | 요구사항 |
|---|---|
| FR-03-01 | Gemini API를 이용해 메일 본문에서 품목명, 수량, 구매일을 구조화된 JSON으로 추출한다. |
| FR-03-02 | 쇼핑몰별 규칙 기반 파서를 별도로 구현하지 않는다 (템플릿 변경에 대응하기 위함). |
| FR-03-03 | 파싱에 사용된 **원본 메일 본문은 저장하지 않는다.** 구조화된 결과만 DB에 저장한다. |
| FR-03-04 | 재처리가 필요한 경우, 저장된 사본이 아니라 메일 고유 ID로 원본 메일함(Gmail 또는 네이버메일)에서 다시 조회한다 (사용자의 메일함 자체를 원본 저장소로 취급). |

#### FR-04. 재고 관리

| ID | 요구사항 |
|---|---|
| FR-04-01 | 재고는 **가구(Household) 단위**로 공유된다. 구성원 누구나 조회·수정 가능하다. |
| FR-04-02 | 소진 우선순위는 **보관 방식별 상대 경과율** 순으로 정렬한다. 구매일만 보는 순수 FIFO는 냉동식품을 신선채소보다 먼저 먹으라고 안내하는 오류를 낳는다(실사용에서 확인 — Q1이 예상한 상황). `경과율 = 구매 후 경과일 ÷ 보관방식별 기준일수`로 계산하고, 기준일수는 하드코딩하지 않고 조정 가능하게 둔다. |
| FR-04-03 | 유통기한 데이터는 별도로 관리하지 않는다 (주문 메일에 없는 정보). FR-04-02의 기준일수는 유통기한이 아니라 **보관 방식에 따른 소진 속도 가정**이며, 정확한 날짜를 주장하지 않는다. |
| FR-04-04 | 재고 항목은 **보관 방식(냉장/냉동/실온/미상)** 을 가진다. 상품명에 `(냉장)`·`(냉동)` 등이 포함된 경우가 많으므로 메일 파싱 단계에서 함께 추출하고, 없으면 재료 종류로 추정한다. 추정이 불가능하면 `미상`으로 두고 FR-04-02에서 실온에 준해 다룬다. |
| FR-04-05 | 사용자는 재고 화면에서 보관 방식을 직접 고칠 수 있다 (추출·추정이 틀릴 수 있으므로). |
| FR-04-09 | **재고 한 행이 몇 끼분인지 다룬다.** 레시피는 1인분 기준이고 한 끼에 양파 5g·대파 10g씩 쓰는데, 재고 한 행을 "한 번 쓰면 사라지는 것"으로 보면 재고 9줄이 요리 5개 만에 바닥나 뒤쪽 식단표가 통째로 0%가 된다(실측 71%). 순서는 ① 사용자가 입력한 개수 → ② **수량 표기에서 읽어낸 개수**("6개"→6, "330g, 5개"→5) → ③ 1끼분이다. 무게·부피만 있는 표기는 개수로 환산하지 않는다(FR-05-04). 남은 비율(FR-05-03)을 곱하되 조금이라도 남았으면 최소 1끼분으로 본다. `1망`·`1봉`처럼 **개수가 천차만별인 표기**만 사용자에게 물어본다 — 다 물어보면 알림이 시끄러워져 정작 중요한 것을 무시하게 된다. |
| FR-04-08 | **재고 항목의 재료명·수량·구매일을 사용자가 고칠 수 있다.** 메일 파싱(LLM 추론)도 수동 입력도 값을 틀리게 남길 수 있는데, 특히 **재료명이 어긋나면 매칭이 오류 없이 0건**이 되어 화면에는 "맞는 레시피가 없다"로만 보인다. 자동화가 만든 데이터에는 사람이 고칠 탈출구가 있어야 한다. 재료명은 FR-04-07의 자동완성을 그대로 재사용하고, 이름을 빈 값으로 지우는 것은 막는다(빈 이름은 매칭에서 영영 빠지는데 화면상 빈 줄로만 남는다). 구매일 수정은 경과율을 바꿔 목록 순서와 "먼저 드세요" 판단까지 움직인다. |
| FR-04-06 | 사용자는 재고를 **직접 추가**할 수 있다. 메일 파싱은 등록 쇼핑몰의 온라인 주문만 잡으므로 마트·시장에서 산 것, 선물받은 것, 미등록 쇼핑몰 주문이 통째로 빠진다. 재고가 실제 냉장고와 어긋나면 그 위에 세워진 레시피 추천과 식단표가 함께 틀리므로, 자동화가 놓친 것을 메우는 **탈출구**가 필요하다. §2.1의 "수동 입력이 **거의** 없어야 한다"에서 예외에 해당하며, 기본 경로가 되어서는 안 된다. |
| FR-04-07 | 수동 추가 시 재료명은 **레시피 재료 어휘(`recipe_ingredient.normalized_name`)에서 자동완성**한다. 사용자가 "쇠고기"처럼 다르게 적으면 레시피 매칭이 오류 없이 조용히 0건이 되므로, 이미 확보된 어휘에서 고르게 해 그 실패를 원천 차단한다. 목록에 없는 재료도 입력할 수 있되 매칭이 안 될 수 있음을 알린다. |

#### FR-05. 소진(사용) 처리

| ID | 요구사항 |
|---|---|
| FR-05-01 | 추천 레시피를 "요리함" 처리하면, 매칭된 주재료 목록이 **체크리스트**로 표시된다. 기본값은 전부 체크(=전량 소진)이며, 사용자가 예외 항목만 체크 해제하거나 **남은 양을 조정**한다. |
| FR-05-02 | 레시피와 무관하게 개별적으로 소진된 재료는 사용자가 재고 화면에서 직접 처리할 수 있다. |
| FR-05-03 | 소진은 **분수 단위(전량 / 1/2 / 1/3 남김)** 로 처리한다. 한 번에 다 쓰지 않는 재료가 흔해 전량/미소진 2단계로는 재고가 현실과 어긋나고, 그 재고 위에 레시피 추천과 식단표가 세워진다. |
| FR-05-04 | **단위 환산은 여전히 하지 않는다.** "500g 중 200g"처럼 실단위를 다루지 않고, 남은 비율만 기록한다 — 분수는 단위 환산 없이 정확도를 올리는 중간 지점이다. 남은 비율이 0이 되면 소진 완료로 처리한다. |

#### FR-19. 재료 시각화 (아이콘/일러스트) 시스템

> v1.2에서 화면구성 논의를 반영해 추가된 요구사항이며, 재고 관리(FR-04·FR-05)와 직접 연관되어 Phase 1 범위에 포함한다.

| ID | 요구사항 |
|---|---|
| FR-19-01 | 재고 항목·레시피 재료는 `normalizedName` 기준으로 시각 자료(이모지 또는 AI 생성 일러스트)를 매핑해 표시한다 (§4.2). |
| FR-19-02 | 매핑에 없는 재료는 카테고리 아이콘(채소/유제품/육류/수산/곡물 등)으로 폴백한다. |

---

### Phase 2 — 레시피 추천

#### FR-06. 레시피 데이터 소스

| ID | 요구사항 |
|---|---|
| FR-06-01 | 식품의약품안전처 "조리식품의 레시피 DB"(COOKRCP01) 공공 API를 1차 레시피 소스로 사용한다. 무료이며 칼로리·탄단지·나트륨 등 영양정보가 내장되어 있다. |
| FR-06-02 | 레시피 소스는 추상화 레이어로 설계해, 추후 다른 공공 API나 민간 API(예: 만개의레시피)를 소스로 추가할 수 있게 한다. |
| FR-06-04 | **소스의 부가 정보를 함께 수집·표시한다.** 요리종류·조리방법(`RCP_PAT2`/`RCP_WAY2`, 100%)은 상세 화면 배지로, 나트륨 저감 조리 팁(`RCP_NA_TIP`, 95%)은 조리법 다음에, 1인분 중량(`INFO_WGT`, 25%)은 영양정보의 기준량으로 쓴다. |
| FR-06-03 | **레시피 사진을 수집·표시한다.** 완성 사진(`ATT_FILE_NO_MAIN`)은 목록 카드 썸네일과 상세 상단에, **조리 단계별 사진(`MANUAL_IMG01~20`)은 각 단계의 글과 한 쌍으로** 보여준다. 글과 사진은 **한 객체(`{text, imageUrl}`)로 묶어 저장한다** — 두 배열을 인덱스로 맞추는 구조는 한쪽이 어긋나는 순간 3단계 사진이 2단계에 붙어도 아무 오류 없이 잘못된 조리법이 된다. 원본이 http로 내려오므로 저장 시점에 https로 올린다(안 그러면 브라우저가 혼합 콘텐츠로 막아 이미지가 **조용히** 안 뜬다). 이미지는 원본 서버를 그대로 참조하며 복사해 두지 않으므로, 불러오기에 실패하면 기존 재료 아이콘(FR-19)으로 폴백한다. |

#### FR-07. 레시피 구조화

| ID | 요구사항 |
|---|---|
| FR-07-01 | 식약처 API의 재료 필드(`RCP_PARTS_DTLS`)는 비정형 텍스트이므로, 레시피를 DB로 수집하는 **시점(배치, 1회성)**에 Gemini API로 주재료/부재료(양념)를 분류하고 재료명을 정규화해 구조화된 형태로 저장한다. (런타임마다 LLM을 호출하지 않음) |
| FR-07-02 | 소금·설탕·간장·식용유·다진마늘·참기름 등 "기본 보유로 가정 가능한" 조미료 화이트리스트를 관리하고, 매칭률 계산에서 제외한다. |

#### FR-08. 레시피 매칭 스코어링

| ID | 요구사항 |
|---|---|
| FR-08-01 | 매칭 점수는 다음 공식을 기본으로 한다 (가중치는 실사용 후 튜닝 가능하도록 하드코딩하지 않음): |

```
score = (보유 주재료 수 / 필요 주재료 수) × 0.6
      + (레시피 주재료 중 소진임박 TOP N 포함 비율) × 0.4
※ 부재료(조미료)는 화이트리스트 처리되어 매칭 계산에서 제외
```

| ID | 요구사항 |
|---|---|
| FR-08-02 | 완전 매칭이 아니어도 부분 매칭 레시피를 추천 대상에 포함한다. 부족한 주재료는 "부족 재료"로 표시한다. |

#### FR-09. 레시피 노출

| ID | 요구사항 |
|---|---|
| FR-09-01 | 매일 자동으로 "오늘의 추천 레시피"를 큐레이션한다 (재고 자동 갱신 주기와 연동해 하루 단위로 고정). |
| FR-09-02 | 사용자가 언제든 조회할 수 있는 온디맨드 전체 레시피 목록도 제공한다. |

#### FR-10. 밀키트 유도 (UI 훅)

| ID | 요구사항 |
|---|---|
| FR-10-01 | 매칭률이 애매한 구간(예: 40~70%)의 레시피에는 "밀키트로 간편하게 만들어보기" CTA를 노출한다. |
| FR-10-02 | 실제 상품 검색·연결 로직은 Phase 4에서 구현한다. v1에서는 UI 자리만 마련한다. |

---

### Phase 3 — 주간 식단표

#### FR-11. 식단표 범위

| ID | 요구사항 |
|---|---|
| FR-11-01 | **평일(월~금) 5일 저녁 + 주말·공휴일 점심 및 저녁**을 기본 범위로 한다. (끼니 수는 그 주의 공휴일 존재 여부에 따라 가변적이다.) |
| FR-11-02 | 공휴일 판단은 **한국천문연구원 특일 정보 API(공공데이터포털)** 로 자동 인식한다. 사용자가 수동으로 공휴일을 표시할 필요가 없다. |
| FR-11-03 | 사용자별로 원하는 범위(끼니 구성)를 커스텀하는 기능은 v1에서 제외한다 (고도화 과제, §10). |

#### FR-12. 식단표 생성/편집

| ID | 요구사항 |
|---|---|
| FR-12-01 | 주간 시작 시 전체 끼니를 자동 생성한다. |
| FR-12-02 | 개별 끼니를 다른 추천 레시피로 스왑할 수 있다. |
| FR-12-03 | 자동 추천 목록에 없는 레시피도 사용자가 직접 골라서 끼니에 채울 수 있다 (자유 편집). |

#### FR-13. 자동 배치 로직

| ID | 요구사항 |
|---|---|
| FR-13-01 | 요일 순서대로 **순차(연쇄) 계산**한다. 앞선 요일에 배치된 레시피가 사용한 주재료는 다음 요일 계산 시 이미 소진된 것으로 반영한다. |
| FR-13-02 | 같은 주 안에서 **동일 레시피의 중복 배치를 금지**한다. |
| FR-13-03 | 최선의 후보조차 매칭률이 매우 낮더라도(예: 10~20%) **최소 기준 없이 무조건 1위 후보를 배치**한다 (빈 칸으로 남기지 않음 — "무엇을 먹을지"가 정해져야 장보기 행동으로 이어지기 때문). |
| FR-13-04 | 매칭률이 낮은 상황(=장보기가 필요한 상황)일수록, **최근 구매 이력 대비 비중이 낮은 카테고리·단백질원에 가산점**을 주는 "다양성 보너스"를 스코어링에 반영한다. 이는 구매 이력 데이터만으로 판단 가능한 **규칙 기반 다양성 유도**이며, 아래 스코어링 공식을 사용한다: |

```
score = 매칭률 × 0.5
      + 소진임박 가중치 × 0.3
      + 카테고리 다양성 보너스 × 0.2
        (최근 4주 구매 이력에서 비중 낮은 카테고리일수록 가산점)
```

| ID | 요구사항 |
|---|---|
| FR-13-05 | 부족한 주재료는 "장보기 후보"로 표시하고, 미리 주문하도록 유도하는 CTA를 노출한다 (Phase 4 쇼핑 연결로 이어지는 지점). |
| FR-13-06 | **간식(후식)은 끼니 칸에 자동 배치하지 않는다.** 판정은 레시피 소스가 매긴 요리종류(식약처 `RCP_PAT2`)를 그대로 쓰며, `후식`으로 분류된 것을 제외한다. 같은 이유로 오늘의 추천(FR-09-01)에서도 뺀다. 다만 레시피 탭 목록에서는 감추지 않고 분류 배지를 붙여 구분만 하며, 사용자가 직접 고르면(FR-12-03) 끼니에 넣을 수 있다. 분류를 모르는 레시피(`null`)는 **끼니로 본다** — 후보 풀이 비어 빈 칸이 생기는 것(FR-13-03 위반)이 간식이 한 번 뜨는 것보다 나쁘기 때문이다. |
| FR-13-08 | **한 끼니를 한식 상차림으로 차린다.** 요리 하나만 배치하면 저녁이 "반찬 한 접시"로 끝나 실제로 그렇게 먹지 않는다. 첫 요리의 요리종류가 `일품`·`밥`이면 그 하나로 끝내고(볶음밥·카레는 곁들일 필요가 없다), 아니면 **국·찌개 1 + 반찬 N**(기본 2)을 붙인다. 맨밥은 레시피가 아니므로 뽑지 않고 "당연히 있는 것"으로 친다. 연쇄 계산(FR-13-01)은 **요리 단위**로 흐른다 — 같은 끼니 안에서도 국이 쓴 두부는 그 뒤 반찬 입장에서 이미 없는 셈이다. 그 자리에 맞는 후보가 고갈되면 **그 자리만 비운다** — 국이 없다고 반찬까지 못 놓는 것보다 있는 만큼 차리는 편이 낫다. 교체(FR-12-02)와 직접선택(FR-12-03)은 끼니가 아니라 **요리 단위**로 일어난다(국만 바꾸고 반찬은 두고 싶은 게 자연스럽다). 영양 합계(FR-14-01)도 요리 단위로 더한다. |
| FR-13-09 | **한 주에 같은 재료가 쏠리지 않게 한다.** 재고에 두부가 있으면 후보 레시피 대부분이 두부를 쓰게 되어 한 주가 통째로 두부 요리로 깔린다. 앞에서 다 써버려 뒤쪽 끼니는 전부 0%가 되고 장보기 목록도 한쪽으로 쏠린다. 이번 주에 이미 임계(기본 2회) 이상 쓰인 재료를 쓰는 레시피의 점수를 깎는다. **임계를 1로 낮추면 오히려 나빠진다** — 모든 재료가 첫 사용부터 감점되어 감점이 상수가 되고 순위가 감점 전과 같아진다(실측 확인). 동점 처리에서도 덜 쓴 재료를 앞세운다. |
| FR-13-10 | **간편조리식(밀키트·즉석국) 자리를 한 주에 N끼(기본 2) 배치한다.** 레시피 소스에는 "사서 데우기만 하는 것"이 없지만 실제 장보기에서는 큰 몫이다 — 곰탕 한 팩을 사두면 바쁜 날 한 끼가 되고, 그래서 미리 쟁여 두려고 온라인 장을 본다. 한 주를 전부 직접 만드는 요리로 채우면 며칠 만에 버려지는 식단표가 되므로 몇 끼는 간편식 자리로 남긴다. 간편식은 **레시피가 아니라 장바구니 후보**라 recipe 테이블에 넣지 않고 코드 목록(`lib/meal-plan/convenience.ts`)의 키만 저장한다 — 필요한 건 이름과 검색어뿐인데 레시피 테이블은 재료·조리법·영양을 요구한다. 재료를 쓰지 않으므로 가상 재고를 건드리지 않고, 매칭 막대도 그리지 않는다(그리면 "있는 재료로 만들 수 있다"는 거짓말이 된다). 자리는 주 안에 **고르게 흩뿌리고**(몰아 두면 "요리 안 하는 주"로 보인다), 주차에 따라 품목을 회전시킨다(같은 곰탕만 계속 뜨면 이미 사둔 사람에게 쓸모없다). 커머스 키가 붙으면 `searchQuery`가 그대로 상품 검색어가 된다(FR-15). |
| FR-13-07 | **제철이 아닌 재료를 사야 하는 레시피는 감점한다.** 감점은 세 항의 가중합에 **곱한다** — 네 번째 항으로 더하면 다른 항이 높은 레시피가 감점을 흡수해 한여름 감귤이 여전히 상위에 남는다. 감점 대상은 **부족한 주재료뿐이며, 이미 보유한 재료는 제철이 아니어도 감점하지 않는다** (냉장고의 감귤은 8월이어도 먼저 먹어 없애야 할 재고이므로, 감점하면 FR-04-02와 정면으로 부딪힌다). 완전 배제가 아니라 감점인 이유는 FR-13-03이 빈 칸을 금지하기 때문이다. 제철 판정 대상은 **계절을 확실히 타는 재료로 한정**하며, 건조·가공품(곶감·건포도·김·멸치), 저장·시설재배 채소(감자·배추·애호박), 재배 버섯, 수입 과일은 연중으로 본다. 목록에 없는 재료는 언제나 제철로 취급한다. |

#### FR-14. 영양 정보 요약

| ID | 요구사항 |
|---|---|
| FR-14-01 | 식단표 하단에 주간 합계 칼로리·탄수화물·단백질·지방·나트륨을 **정보 제공 수준**으로 표시한다. |
| FR-14-02 | 목표치 대비 비교나 개인 맞춤 권장량 계산은 하지 않는다 (고도화 과제, §10). |

---

### Phase 4 — 쇼핑 연결

#### FR-15. 커머스 플랫폼 연동

| ID | 요구사항 |
|---|---|
| FR-15-01 | **쿠팡 파트너스 API**를 메인 수익화 채널로 정식 연동한다. 자동화된 제휴 링크 생성이 공식적으로 허용된다. |
| FR-15-02 | **네이버쇼핑 검색 API**(무료, 수수료 없음)를 상품 비교 노출용 보조 채널로 활용한다. |
| FR-15-03 | 네이버 쇼핑 커넥트(제휴 수수료 최대 50%)는 자동화 프로그램을 공식 지원하지 않으므로 **메인 수익 구조로 사용하지 않는다.** 소규모 실험 여부는 Open Question으로 남긴다. |

#### FR-16. 연결 상품 유형

| ID | 요구사항 |
|---|---|
| FR-16-01 | 부족한 재료가 있을 때 "재료 낱개로 채우기"와 "밀키트로 한번에 해결하기" **두 옵션을 함께 제시**한다. |
| FR-16-02 | 밀키트/낱개 재료 선호도에 따른 개인화는 v1에서 제외한다 (고도화 과제, §10). |

#### FR-17. 노출 단위

| ID | 요구사항 |
|---|---|
| FR-17-01 | 레시피 상세 화면에 그 레시피의 부족 재료/밀키트 링크를 개별 노출한다. |
| FR-17-02 | 한 주 식단표 전체의 부족 재료를 모은 **"장보기 리스트"** 화면을 별도로 제공한다. |
| FR-17-03 | 장보기 리스트에서 여러 레시피가 같은 재료를 필요로 하는 경우, **재료 기준으로 통합 표시**한다 (예: "대파 — 2개 레시피에서 사용"). 정확한 필요 수량 합산은 하지 않는다 (FR-05-03과 일관된 단순화 원칙). |

#### FR-18. 결제 관련 제약 (명시적 비목표)

| ID | 요구사항 |
|---|---|
| FR-18-01 | 쿠팡·네이버쇼핑 모두 서드파티가 실제 장바구니 담기/결제를 수행할 수 있는 공개 API를 제공하지 않는다. 서비스는 **상품 페이지로의 딥링크 제공까지만** 담당하며, 장바구니 담기·결제는 사용자가 쿠팡/네이버 앱·사이트에서 직접 완료한다. |
| FR-18-02 | 여러 상품을 한 번에 장바구니에 담아주는 기능은 제공하지 않는다. |

---

## 6. 데이터 모델

```ts
/** 가구 */
interface Household {
  id: string;
  name: string;
  createdAt: string;
}

/** 가구 구성원 (로그인 계정) */
interface Member {
  id: string;
  householdId: string;
  googleAccountId: string;
  displayName: string;
  role: "owner" | "member";
}

/** 가구에 연결된 메일 계정 (Gmail 또는 네이버메일, 재고 데이터 소스이며 로그인 계정과 별개일 수 있음) */
interface MailConnection {
  id: string;
  householdId: string;
  connectedByMemberId: string;
  provider: "gmail" | "naver";
  emailAddress: string;
  // provider별 인증 정보 (항상 암호화 저장)
  auth:
    | { type: "oauth"; encryptedRefreshToken: string }        // Gmail
    | { type: "imap_app_password"; encryptedAppPassword: string }; // 네이버메일
  lastSyncedAt: string | null;
  status: "active" | "expired" | "revoked";
}

/** 처리된 메일 (멱등성 보장용, 원본 본문은 저장하지 않음) */
interface ProcessedMailRecord {
  id: string;
  mailConnectionId: string;
  providerMessageId: string;       // Gmail 메시지 ID 또는 IMAP UID — 멱등성 키
  processedAt: string;
  extractionStatus: "success" | "failed" | "partial";
}

/** 재고 아이템 */
interface InventoryItem {
  id: string;
  householdId: string;
  normalizedName: string;          // LLM이 정규화한 재료명 (예: "우유")
  rawName: string;                 // 원 상품명 (참고용)
  quantity: string;                // 자유 텍스트 (예: "1L", "1모") — 정밀 단위 환산 없음
  purchasedAt: string;             // 경과율 계산 기준 (FR-04-02)
  storageType: "refrigerated" | "frozen" | "room_temp" | "unknown"; // FR-04-04
  remainingFraction: number;       // 1 = 미개봉, 0.5 = 반 남음, 0 = 소진 (FR-05-03)
  sourceMailConnectionId: string;
  status: "in_stock" | "consumed";
  consumedAt: string | null;
  consumedVia: "recipe_cooked" | "manual" | null;
}

/** 재료 시각 자료 매핑 (§4.2) */
interface IngredientIconMap {
  normalizedName: string;          // InventoryItem/RecipeIngredient의 normalizedName과 동일 키
  visualType: "emoji" | "generated_illustration" | "category_fallback";
  assetRef: string;                // 이모지 유니코드, 일러스트 파일 경로, 또는 카테고리 아이콘 키
  category: "vegetable" | "dairy" | "meat" | "seafood" | "grain" | "seasoning" | "other";
}

/** 레시피 (식약처 API 원본 + LLM 구조화 결과) */
interface Recipe {
  id: string;
  sourceApi: "foodsafetykorea_cookrcp01" | string; // 확장 가능
  sourceRecipeId: string;
  name: string;
  imageUrl: string | null;
  instructions: string[];
  nutrition: {
    calories: number;
    carbohydrate: number;
    protein: number;
    fat: number;
    sodium: number;
  };
  ingredients: RecipeIngredient[];
}

interface RecipeIngredient {
  normalizedName: string;
  role: "main" | "seasoning";      // LLM이 1회 분류
  isWhitelistedSeasoning: boolean; // 조미료 화이트리스트 매칭 여부
}

/** 주간 식단표 */
interface WeeklyMealPlan {
  id: string;
  householdId: string;
  weekStartDate: string;
  entries: MealPlanEntry[];
}

interface MealPlanEntry {
  date: string;
  mealType: "dinner" | "lunch";
  isHoliday: boolean;              // 특일정보 API 기준
  recipeId: string;
  matchScore: number;
  missingMainIngredients: string[];
  source: "auto" | "swapped" | "manual";
}

/** 장보기 리스트 (재료 기준 통합) */
interface ShoppingListItem {
  normalizedName: string;
  usedInRecipeIds: string[];       // 이 재료를 필요로 하는 레시피들
  coupangDeepLink: string | null;
  naverShoppingLink: string | null;
  mealKitOptions: {
    recipeId: string;
    coupangDeepLink: string | null;
  }[];
}
```

---

## 7. 기술 스택

| 영역 | 선택 | 근거 |
|---|---|---|
| 프레임워크 | Next.js 15 (App Router) + TypeScript | API Routes로 백엔드 겸용 |
| DB | Supabase (Postgres) | 가구-구성원-메일연결-재고의 관계형 구조에 적합. Row-Level Security로 가구별 데이터 격리를 DB 레벨에서 강제 |
| 인증 | Supabase Auth (Google OAuth) | 로그인용 계정과 Gmail 데이터 소스용 계정을 분리 관리 |
| 이메일 연동 | Gmail API (OAuth) + ImapFlow(Node.js IMAP 클라이언트, 네이버메일) | 프로바이더별 어댑터 패턴으로 두 소스를 동일한 파싱 파이프라인에 연결 |
| 이메일 파싱 | Gemini API | LLM 기반 구조화 추출 (프로바이더 무관 공통 로직) |
| 레시피 데이터 | 식약처 COOKRCP01 API | 무료, 영양정보 내장 |
| 스케줄링 | Vercel Cron Jobs | 매일 자동 폴링 |
| 커머스 | 쿠팡 파트너스 API + 네이버쇼핑 검색 API | §5 FR-15 참고 |
| 공휴일 | 한국천문연구원 특일 정보 API (공공데이터포털) | 식단표 공휴일 자동 인식 |
| 상태 관리 | TanStack Query + Zustand | |
| 스타일 | Tailwind CSS | |
| 재료 시각 자료 | Twemoji/OpenMoji(SVG) + AI 생성 일러스트(한식 특화 재료) | §4.2 하이브리드 전략 |
| 배포 | Vercel | |

---

## 8. 보안 및 개인정보 보호 요구사항

이 서비스는 타인(배우자 포함)의 이메일 데이터를 처리하며, 지인 확장과 비즈니스화를 계획하고 있어 개인정보 보호 설계를 아키텍처 초기 단계부터 반영한다.

| ID | 요구사항 |
|---|---|
| NFR-01 | Gmail 접근 범위는 `category:purchases` + 등록 쇼핑몰 도메인으로, 네이버메일 접근 범위는 IMAP `SEARCH FROM` 발신 도메인 필터로 각각 최소화한다 (두 프로바이더 모두 전체 편지함 접근 금지). |
| NFR-02 | 원본 메일 본문은 프로바이더와 무관하게 저장하지 않는다. LLM 파싱 후 구조화된 데이터만 보관한다. |
| NFR-03 | Gmail OAuth 리프레시 토큰과 네이버메일 IMAP 앱 비밀번호는 모두 암호화된 상태로 저장한다. |
| NFR-04 | 가구 데이터는 Supabase Row-Level Security로 격리해, 다른 가구의 데이터에 접근할 수 없도록 한다. |
| NFR-05 | `gmail.readonly`는 Google이 분류한 민감 범위(Restricted Scope)로, 사용자 수가 늘어나면 Google의 OAuth 앱 인증 심사(경우에 따라 CASA 보안 평가)가 필요하다. 지인 확장 단계 진입 전 이 심사 절차를 마일스톤에 반영한다 (§9). 네이버메일 IMAP은 이런 플랫폼 차원의 앱 심사 절차가 없어 이 부담에서 자유롭다. |
| NFR-06 | 실서비스 전환(지인 확장) 시점에 개인정보처리방침·이용약관을 마련한다. 배우자 등 제3자의 메일(Gmail 또는 네이버메일)을 연동하는 경우, 해당 계정 소유자 본인이 직접 인증(OAuth 또는 앱 비밀번호 입력)을 거치도록 해 본인 동의를 명확히 확보한다. |

---

## 9. 개발 마일스톤

Phase 1~4는 데이터가 순서대로 쌓여야 다음 Phase가 성립하는 의존 구조다 (재고 없이는 레시피 매칭이 무의미, 레시피 없이는 식단표를, 식단표의 "부족 재료" 없이는 쇼핑 연결을 만들 수 없음). 따라서 개발 순서도 Phase 순서를 그대로 따르며, 각 단계마다 **기능(백엔드 로직)과 화면(§4 IA 기준)을 함께** 완성해 "동작하는 화면"이 매 단계마다 존재하도록 한다.

| 단계 | 기능 산출물 | 화면 산출물 (§4 기준) | 완료 기준 |
|---|---|---|---|
| **M0. 셋업** | 프로젝트 스캐폴딩, Supabase 스키마, Google OAuth 앱 등록(테스트 모드) | 사이트맵 확정(§4.1), 디자인 토큰(팔레트·타이포) 확정, 재료 아이콘/일러스트 리소스 1차 확보(§4.2) | 가구/구성원 CRUD 동작 + 기본 디자인 시스템 컴포넌트 준비 |
| **M1. Phase 1 — 메일 → 재고** | Gmail(OAuth)·네이버메일(IMAP) 병렬 연동, 발신 필터링, LLM 파싱, FIFO 재고, Vercel Cron, 멱등성 처리 | 온보딩 플로우 전체(스플래시~메일연동 가이드~완료), 홈 탭 기본, **재고 탭**(FIFO 순 리스트 + 아이콘 카드, §4.2), 설정 화면(메일 계정 관리) | 실제 주문 메일로 재고가 자동 반영되고, 재고 탭에서 소진임박 순서가 시각적으로 확인됨 |
| **M2. Phase 2 — 레시피 추천** | 식약처 API 연동, LLM 배치 구조화(주/부재료), 매칭 스코어링, 일일 큐레이션 | **레시피 탭**(목록/상세), 요리함 체크리스트 모달, 홈 탭 "오늘의 추천" 카드, 밀키트 유도 CTA(훅만) | "요리함" 처리 시 재고가 정확히 갱신되고, 홈에서 추천 레시피로 바로 진입 가능 |
| **M3. Phase 3 — 주간 식단표** | 특일정보 API 연동, 순차 배치 로직, 다양성 보너스, 영양 요약 | **식단표 탭**(주간 캘린더 뷰), 끼니 스왑/직접선택 모달, 영양정보 요약 카드 | 한 주치 식단표가 자동 생성·편집 가능 |
| **M4. Phase 4 — 쇼핑 연결** | 쿠팡 파트너스/네이버쇼핑 연동, 장보기 리스트 재료 통합 로직 | **장보기 탭**(통합 리스트), 레시피 상세 내 부족재료 개별 CTA | 부족 재료 클릭 시 실제 상품 페이지로 이동 |
| **M5. 보안 심사 준비** | Google OAuth 앱 인증 신청, 개인정보처리방침 작성 | (화면 변경 없음) | 지인 초대 가능 상태 |
| **M6. 지인 확장** | 초대 코드 기반 가구 합류 플로우 | 초대 코드 입력/발급 화면 | 외부 사용자(신혼부부 등) 온보딩 완료 |

> **순서에 대한 메모**: M0의 "재료 아이콘/일러스트 1차 확보"는 이모지 세트(즉시 사용 가능)만으로 M1을 시작하기에 충분하며, 한식 특화 일러스트(AI 생성 20%)는 M1 진행과 병행해도 무방하다 — 아이콘이 전부 준비돼야만 재고 탭 개발을 시작할 수 있는 것은 아니다. 보안 심사(M5)는 실사용자가 본인 외 1인(배우자) 이상으로 늘어나는 시점에 필요하므로 M1 완료 직후부터 병행 준비하는 것을 권장한다.

---

## 10. 열린 질문 (Open Questions) — 고도화 과제

| # | 질문 | 비고 |
|---|---|---|
| ~~Q1~~ | ~~FIFO에 신선/상온 카테고리 가중치를 추가할 것인가?~~ → **해결됨**: 실사용에서 냉동식품이 신선채소보다 먼저 먹으라고 안내되는 오류가 확인되어 FR-04-02·FR-04-04로 반영 | 상품명에 `(냉장)`/`(냉동)` 표기가 이미 들어 있어 파싱 단계에서 확보 가능했다 |
| Q2 | 알러지·기피 재료·식단 유형(비건/저염 등) 개인화 | Phase 1~4 기본 골격 완료 후 |
| Q3 | 레시피 수용/거부 피드백 기반 입맛 학습 | 별도 추천 모델 필요, 고도화 단계 |
| Q4 | 밀키트 vs 낱개 재료 선호 개인화 | acceptance 데이터 축적 후 |
| Q5 | 유저별 식단표 범위(끼니 구성) 커스텀 | 고도화 단계 |
| Q6 | 네이버 쇼핑 커넥트 소규모 테스트 여부 | 약관 리스크 고려해 메인 채널로는 사용하지 않음 |
| Q7 | 에이전틱 체크아웃(Google AP2, OpenAI ACP 등) 국내 이커머스 도입 시 자동 결제 연동 | 2026년 8월 기준 국내 미도입, 업계 표준 확립 여부 지속 모니터링 |
| Q8 | 즐겨찾기·레시피 저장 기능 | Phase 2 이후 |
| Q9 | Household 초대 시 권한 세분화 (읽기 전용 등) | 현재는 전 구성원 동일 권한 |
| Q10 | **타깃 페르소나를 "5060대 여성 주부" 기준으로 전면 재검토할 것인가?** 다양성 보너스(색다른 재료 유도), 밀키트 연결, 자유 편집 UI 등 §2·§5 전반의 기능 설계가 "신혼부부·맞벌이" 가정하에 이뤄져 있어, 실제 타깃과 맞는지 별도 세션에서 재검토 필요 | 시각 디자인 톤(§4.3, 쉬운 앱+세련됨)과 메일 연동 온보딩 마법사(FR-01-04)는 이 재검토를 기다리지 않고 우선 반영함. 남은 재검토 대상은 기능/정보구조 수준 |
| Q11 | **"처음부터 다시 스캔" 기능 (메일 재수집)** — 동기화는 `mail_connection.last_synced_at`을 워터마크로 삼아 그 이후 메일만 조회한다. 그래서 메일 탐색 로직에 결함이 있었던 기간의 메일은 수정 후에도 영영 조회 범위 밖에 남는다. 설정 화면에서 워터마크를 초기화해 재수집하는 수단이 필요한지 검토 | M1 개발 중 실제로 발생(네이버가 주문 메일을 `청구·결제` 폴더로 분류하는 것을 놓쳐 0건 처리 후 워터마크가 전진). 현재는 운영자가 DB에서 직접 초기화해야 함. 재수집은 이미 처리한 메일을 다시 LLM에 태우므로 비용·중복 처리(멱등성은 `processed_mail_record`가 보장) 관점에서 범위 설계 필요 |
| Q12 | **영수증 OCR로 재고 추가 (M4 이후 착수 예정)** — 메일 파싱이 못 잡는 오프라인 장보기(마트·시장)를 사진 한 장으로 넣는다. 타이핑을 없앤다는 §1.3 컨셉과 정합하지만, 카메라·이미지 저장·비전 모델 호출·인식 결과 보정 UI가 모두 필요해 수동 추가(FR-04-06)와는 규모가 다르다. 별도 단계로 다룬다 | 보정 UI는 수동 추가 화면과 뿌리가 같으므로 FR-04-06을 먼저 만들어 두면 그 위에 얹을 수 있다. 한국 영수증은 상품명이 축약·비표준이라(`청상추150`) 정규화 난도가 주문 메일보다 높다는 점을 감안해야 한다 |
| ~~Q13~~ | ~~**재고 항목 직접 수정**~~ → **해결됨**: FR-04-08로 구현 (v2.0). 원래 설명: — 항목을 눌러 고칠 수 있는 것은 현재 남은 양(FR-05-03)과 보관 방식(FR-04-05)뿐이고, **재료명·수량·구매일은 고칠 수 없다.** 메일 파싱은 LLM 추론이라 정규화된 이름이 틀릴 수 있는데(예: 삼겹살 → 돼지고기), 이름이 어긋나면 매칭이 **오류 없이 0건**이 되어 화면에는 "맞는 레시피가 없다"로만 보인다. 자동화가 만든 데이터에는 사람이 고칠 탈출구가 있어야 한다 | 지우고 다시 담는 우회로가 있어 Phase 3을 막지는 않는다. 재료명 수정은 FR-04-07 자동완성(`IngredientCombobox`)을 그대로 재사용한다. 구매일 수정은 경과율을 바꿔 목록 순서와 "먼저 드세요" 판단까지 움직이므로 함께 다룬다 |
| Q14 | **동일 재료 복수 구매의 수량 표현** — `quantity`는 메일 표기를 그대로 담은 자유 문자열(`"5개"`, `"900ml"`)이라 200g 소분 소고기 5팩도 **행 1개**로 들어간다. 표시는 되지만 1팩만 썼을 때 누를 선택지가 없다 — ¼은 1.25팩이라 안 맞고 "다 썼어요"는 남은 4팩까지 지운다. 0/25/50/75/100 눈금은 2·4팩에는 우연히 맞고 3·5팩에는 안 맞는다. 수량을 숫자로 구조화할지, 복수 구매를 행으로 분할할지 결정 필요 | Phase 3은 수량을 읽지 않으므로(매칭은 재료명 기준, 단위 환산은 FR-05-04에서 제외) 식단표를 막지 않는다. 구조화는 단위 환산(FR-05-04)과 경계가 맞닿아 있어 함께 검토하는 편이 낫다. Q13과 같은 화면에서 다뤄진다 |

---

## 부록 A. 사용 API 참고

**Gmail API**
- 인증: Google OAuth 2.0, `gmail.readonly` (Restricted Scope)
- 검색: `category:purchases` (Gmail 2025.09~ 시스템 카테고리), 발신 도메인 필터 병행
- 참고: [Gmail API 문서](https://developers.google.com/workspace/gmail/api)

**네이버메일 IMAP**
- 서버: `imap.naver.com`, 포트 993 (SSL)
- 인증: 네이버메일 설정 > POP3/IMAP 설정 > IMAP 사용 켜기 > 2단계 인증용 앱 비밀번호 발급 후 사용
- 조회: IMAP `SEARCH FROM "도메인"` 명령으로 등록 쇼핑몰 발신 메일만 조회 (전체 편지함 미접근)
- 참고: [네이버메일 고객센터 - IMAP/SMTP 설정](https://mail.naver.com)

**식품의약품안전처 조리식품의 레시피 DB (COOKRCP01)**
- Endpoint: `https://openapi.foodsafetykorea.go.kr/api/{인증키}/COOKRCP01/json/{시작}/{종료}`
- 인증키 발급: data.go.kr
- 주요 필드: `RCP_NM`(메뉴명), `RCP_PARTS_DTLS`(재료정보, 비정형 텍스트), `INFO_ENG`(열량), `INFO_CAR`/`INFO_PRO`/`INFO_FAT`/`INFO_NA`(영양성분), `MANUAL01~20`(조리순서)

**쿠팡 파트너스 API**
- 제휴 링크 자동 생성 지원, 고정 수수료(기본 3%)
- 참고: [쿠팡 파트너스](https://partners.coupang.com)

**네이버쇼핑 검색 API**
- 무료, 상품 검색 결과 제공 (수수료·제휴 기능 없음)
- 참고: [네이버 개발자센터](https://developers.naver.com)

**한국천문연구원 특일 정보 API**
- 공공데이터포털을 통해 인증키 발급
- 공휴일/명절 연휴 정보 제공

---

## 부록 B. 문서 개정 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|---|---|---|---|
| v1.0 | 2026-08-19 | 최초 작성 (Phase 1~4 + 보안 요구사항 포함) | Halina Cho |
| v1.1 | 2026-08-19 | 메일 연동을 Gmail 단독에서 Gmail+네이버메일(IMAP) 병렬 지원으로 확장 (FR-01, 데이터 모델, 기술스택, 보안 요구사항 반영). 5060대 여성 주부 페르소나 재검토를 Open Question(Q10)으로 추가 | Halina Cho |
| v1.2 | 2026-08-19 | §4 정보 구조 및 화면 구성 신설 (사이트맵, 재고 탭 3섹션 구조, 재료 아이콘/일러스트 확보 전략, 디자인 톤). FR-19(재료 시각화 시스템) 추가. §9 개발 마일스톤을 Phase별 기능+화면 산출물 병행 구조로 재정리. 섹션 번호 전체 재정렬(§4 이후 +1) | Halina Cho |
| v1.3 | 2026-08-19 | **재고 탭 3섹션 구조(소진임박도 기준 오늘/이번주/여유) 삭제.** 재고 탭은 FIFO 순 단일 리스트 + 아이콘 카드로 단순화. FR-19-03 제거, §4 서브섹션 번호 재정렬(4.3→4.2, 4.4→4.3) | Halina Cho |
| v1.4 | 2026-08-21 | **M1·M2 실사용 피드백 반영.** 순수 FIFO가 냉동식품을 신선채소보다 먼저 안내하는 오류가 확인되어, 보관 방식별 상대 경과율 정렬로 변경(FR-04-02) 및 보관 방식 필드 추가(FR-04-04·FR-04-05) — Open Question Q1 해결. 소진 처리를 전량/미소진 2단계에서 분수 단위(전량/½/⅓)로 확장(FR-05-03·FR-05-04); 단위 환산 배제 원칙은 유지. 데이터 모델 `InventoryItem`에 `storageType`·`remainingFraction` 추가. 메일 파싱 LLM을 Claude에서 Gemini로 변경(FR-03-01·FR-07-01·§7). 메일 재수집 필요성을 Q11로 추가 | Claude (Halina Cho 위임) |
| v1.5 | 2026-08-21 | **재고 입력 경로 확장.** 메일 파싱이 등록 쇼핑몰의 온라인 주문만 잡아 마트·시장 구매가 통째로 누락되는 문제를 반영해 수동 재고 추가를 추가(FR-04-06). 재료명은 이미 확보된 레시피 재료 어휘에서 자동완성해 표기 불일치로 매칭이 조용히 실패하는 것을 막는다(FR-04-07). 영수증 OCR은 규모가 달라 별도 단계로 분리하고 Q12로 기록 | Claude (Halina Cho 위임) |
| v2.3 | 2026-08-22 | **식단표에 0%가 과하게 많던 문제 (FR-04-09, FR-07-04, FR-13-11).** 레시피 탭에는 100%가 여럿인데 식단표는 0%가 71%였다. 원인은 "재고 1행 = 1회 사용" 모델 — 재고 9줄이 요리 5개 만에 바닥났다. 재료가 이미 "6개"라고 적혀 있는데도 읽지 않고 있었다. 실측으로 0% 71%→50%, 앞 4끼니가 모두 채워짐 | Claude (Halina Cho 위임) |
| v2.2 | 2026-08-22 | **재료 쏠림 완화(FR-13-09)와 간편조리식 자리(FR-13-10).** 상차림으로 요리가 10→20개로 늘자 재고의 특정 재료(두부)에 쏠리는 것이 두드러졌다. 감점 세기를 실측으로 정했다(없음 13회 → 0.7이면 8~9회). 간편식은 "직접 만들긴 어렵지만 사두면 편한 것"을 쟁이려고 온라인 장을 본다는 실사용 관찰에서 나왔다 | Claude (Halina Cho 위임) |
| v2.1 | 2026-08-22 | **한식 상차림 구성 (FR-13-08).** 실사용에서 "한 끼에 반찬 하나만 추천돼 부실하다"는 피드백. 식약처 요리종류(`RCP_PAT2`)에 일품·국&찌개·반찬이 이미 나뉘어 있어 조합할 재료는 충분했다. meal_plan_entry가 계속 요리 하나를 뜻하고, 한 끼니는 (date, meal_type)이 같은 행들의 묶음이 된다(마이그레이션 0010) | Claude (Halina Cho 위임) |
| v2.0 | 2026-08-22 | **재고 항목 수정 (FR-04-08).** 미결로 미뤄뒀던 Q13을 앞당겨 구현. 실사용 중 재고에 `1두부`가 들어가(수량을 이름 칸에 함께 입력) 두부 레시피가 전부 "두부가 없어요"로 뜨는 일이 발생했다 — 이름이 어긋나면 매칭이 오류 없이 0건이 되는, 예고했던 그 실패다. 고칠 방법이 없어 지우고 다시 담는 수밖에 없었다 | Claude (Halina Cho 위임) |
| v1.9 | 2026-08-21 | **버려지던 원본 데이터 전면 회수 (FR-06-04, FR-07-03).** 사진 건과 같은 누락이 재료 쪽에도 있었다 — 재료 원문에 계량이 100% 들어 있는데 LLM 구조화 과정에서 이름만 남기고 계량·그룹·원문을 통째로 버리고 있었다. 미사용 필드 전수 점검 결과 조리 팁(95%)·1인분 중량(25%)·조리방법(100%)도 안 쓰고 있어 함께 회수. 정규화 이름은 매칭의 축이라 손대지 않고 규칙 파서로 얹는 방식을 택했다 | Claude (Halina Cho 위임) |
| v1.8 | 2026-08-21 | **레시피 사진 반영 (FR-06-03).** 완성 사진은 수집 시점부터 DB에 들어와 있었으나 화면에서 전혀 쓰지 않고 있었고, 단계별 사진은 아예 수집하지 않고 있었다. 확인 결과 조리단계 6,717개가 **100%** 사진을 갖고 있어 자료를 그냥 버리고 있던 셈이다. 글과 사진을 한 객체로 묶는 구조를 택했고(인덱스 정렬은 조용히 어긋난다), 외부 호스트 참조라 실패 시 재료 아이콘 폴백을 둔다. 상세 화면 구성은 사진 → 재료 → 조리법(단계별 사진) 순 | Claude (Halina Cho 위임) |
| v1.7 | 2026-08-21 | **간식 구분과 제철 반영.** 재고가 바닥난 요일에 "건강 마늘 꿀환"(식약처 분류 `후식`)이 저녁으로 배치되고 한여름 8월에 감귤 레시피가 추천되는 문제를 확인해 FR-13-06·FR-13-07 추가. 전자는 소스에 이미 있던 `RCP_PAT2`를 수집하지 않고 있던 것이 원인이라 추론 규칙 대신 원본 분류를 그대로 쓴다(마이그레이션 0008 + 백필). 후자는 제철 목록을 새로 만들되, "넣지 말아야 할 것을 넣지 않는 것"을 설계 기준으로 삼았다 — 목록이 잘못 넓어지면 멀쩡한 레시피가 근거 없이 밀려나는데 화면에는 아무 오류도 안 뜬다 | Claude (Halina Cho 위임) |
| v1.6 | 2026-08-21 | **자동화 데이터의 사람 개입 여지를 미결 항목으로 기록.** 재고 항목에서 재료명·수량·구매일을 고칠 수 없는 점(Q13)과 동일 재료 복수 구매를 행 1개로만 표현하는 점(Q14)을 추가. 둘 다 Phase 3(주간 식단표)이 수량을 읽지 않고 이름 오류에는 지우고 다시 담는 우회로가 있어 M3를 막지 않는다고 판단, M4 이후 UI/UX 세션으로 미룸 | Claude (Halina Cho 위임) |
